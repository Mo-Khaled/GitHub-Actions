name: Matrix Hello World

on: 
  push:

jobs:
  hello:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Hello World script
        run: ./script.sh ${{ matrix.os }}
        shell: bash
